import 'package:flutter/material.dart';

/// App color palette — dark streaming theme
class AppColors {
  AppColors._();

  // Backgrounds
  static const Color background = Color(0xFF0D0F14);
  static const Color surface = Color(0xFF181A22);
  static const Color surfaceLight = Color(0xFF222530);
  static const Color card = Color(0xFF1A1D26);
  static const Color bottomNav = Color(0xFF14161E);
  static const Color floatingNav = Color(0xEE1E212B);
  static const Color floatingNavBorder = Color(0x22FFFFFF);

  // Genre & filter chip colors
  static const Color chipSelected = Color(0xFFFFFFFF);
  static const Color chipUnselected = Color(0xFF20232D);
  static const Color chipTextUnselected = Color(0xFFD5D7E0);
  static const Color chipTextSelected = Color(0xFF0D0F14);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF9EA3B0);
  static const Color textHint = Color(0xFF5E6370);

  // Accent
  static const Color star = Color(0xFFFFB800);
  static const Color accent = Color(0xFFE50914);

  // Divider / border
  static const Color divider = Color(0xFF20232D);
}
